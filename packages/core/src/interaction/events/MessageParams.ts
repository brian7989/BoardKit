// Interpolation values for an announcement's message template; the template vocabulary itself
// (defaultMessages) arrives with the a11y layer.
export type MessageParams = Readonly<Record<string, string | number>>;
